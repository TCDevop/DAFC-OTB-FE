'use client';

// This page is the MSAL redirect URI target for Microsoft login popup flow.
// MSAL.js handles the auth code in this window automatically — no manual code needed.
// The popup closes itself after MSAL processes the response.
export default function MicrosoftCallbackPage() {
  return null;
}
